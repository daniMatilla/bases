{
  "name": "bases",
  "version": "0.0.1",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon",
    "build": "rimraf ./dist && tsc",
    "start": "npm run build && node dist/app.js"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.5.1",
    "@types/uuid": "^9.0.2",
    "nodemon": "^3.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "get-age": "^1.0.1",
    "uuid": "^9.0.0",
    "winston": "^3.10.0"
  }
}
